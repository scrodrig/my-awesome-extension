# One awesome extension with React

### For an web extension which renders a pop up in `manifest.json`
```js
 "browser_action": {
     "default_popup": "index.html",
     "default_title": "Weather Widget"
   },
```
